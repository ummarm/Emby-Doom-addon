services:
  emby-doom-addon:
    build: .
    container_name: emby-doom-addon
    restart: unless-stopped
    environment:
      HOST: 0.0.0.0
      PORT: 7000
      EMBY_PROVIDER_IDS: moviebox,streamflix,hindmoviez
    ports:
      - "7001:7000"
